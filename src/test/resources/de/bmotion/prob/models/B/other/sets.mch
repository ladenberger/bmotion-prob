MACHINE sets
CONSTANTS largeSet, setOfSets, simpleRelation, nestedRelation, simpleFunction
PROPERTIES
 largeSet : POW(NAT) & card(largeSet) = 300 &
 setOfSets : POW(POW(NAT)) & card(setOfSets) = 10  &
 simpleRelation : NAT <-> NAT & card(simpleRelation) = 3 &
 nestedRelation: NAT <-> NAT <-> NAT & card(nestedRelation) = 4 &
 simpleFunction:  NAT +-> NAT & card(simpleFunction) = 5
END

